import  Typography  from "@mui/material/Typography";

export const Abstract = ({txt}) => {
  return (
    <Typography variant="h3" sx={{ marginTop:3, marginBottom:3,textAlign:"center" }} >{txt}</Typography>
  )
}
